---
title: "파일 입출력"
date: 2026-07-28
categories: [python, basics]
tags: 코드잇스프린트
---

## 파일 입력
```python
output = "output"

with open('output.txt', 'w', encoding='utf-8') as f:
    f.write(output)
```

## 파일 출력

### Before

```python
import csv

def read_file(filename):
    f = open(filename, 'r')
    lines = f.readlines()
    f.close()

    result = []

    lines.pop(0)

    for l in lines:
        l = l.strip()
        result.append(l.split(","))

    return result
```

### Feedback

| Before | After | Feedback |
| :--- | :--- | :--- |
| `f = open(...)`<br>`f.close()` | `with open(...)` | 예외가 발생해도 파일이 자동으로 닫혀 안전 |
| `lines.pop(0)` | &bull; `f.readlines()[1:]`<br>&bull; `next(f)` | &bull; `pop(0)`의 데이터 대이동 연산을 제거<br>&bull; `next(f)` 적용 시, 메모리를 통째로 쓰지 않는 이터레이터 방식이 되어 성능이 극대화 |
| `open(filename, 'r')` | `encoding='utf-8'` | 한글 깨짐 에러 방지 |

> [!warning]
> `with` 키워드를 사용하거나 `f.close()`를 호출하지 않고 `f.write()`를 호출하면 프로그램이 성공적으로 종료되더라도 `f.write()`의 인자가 디스크에 완전히 기록되지 않을 수 있다.

### After

```python
def read_file(filename):
    with open(filename, 'r', encoding='utf-8') as f:
        next(f)
        return [line.strip().split(",") for line in f]
```

## Reference
* [7. 입력과 출력](https://docs.python.org/ko/3/tutorial/inputoutput.html)
* [5. 자료 구조](https://docs.python.org/ko/3.6/tutorial/datastructures.html)
* [I/O 베이스 클래스](https://docs.python.org/ko/3/library/io.html#io.IOBase)
* [내장 함수](https://docs.python.org/ko/3/library/functions.html#next)