---
title: "시간 계산"
date: 2026-07-07
categories: [python, basics]
---

## 1차 시도

```python
from datetime import datetime


class TimeTracker:
    def __init__(self, start_time, stop_time):
        self.start_time = start_time
        self.stop_time = stop_time

    def start():
    start_time = datetime.now()

    def stop():
    stop_time = datetime.now()
```

> [!warning]
> * 불필요한 생성자
> * `timezone` 설정
> * `self` parameter 누락

## 최종 코드

```python
from datetime import datetime
import pytz


class TimeTracker:
    start_time = ""
    stop_time = ""

    def start(self):
    	self.start_time = datetime.now(pytz.timezone('Asia/Seoul'))
    	return self.start_time

    def stop(self):
    	self.stop_time = datetime.now(pytz.timezone('Asia/Seoul'))
    	return self.stop_time

    def get_elapsed_time(self):
    	return (self.stop_time - self.start_time).total_seconds() / 60

study_session = TimeTracker()
study_session.start()
study_session.stop()
study_session.get_elapsed_time()
```

> [!warning]
> 사용자가 `stop()`을 실행시키지 않았을 경우를 생각해야 한다.
