---
title: "Git Commands"
date: 2026-08-06
categories: git
---

## `git reset`
작업했던 코드를 버리지 않고, Staging Area에 남겨두고 싶을 때

``` bash
git reset --soft [Commit ID]
```

이후 `--force` 키워드를 붙여서 `push`
``` bash
git push --force
```